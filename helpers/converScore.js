function convertScore(arr) {
    hasil = [];
    for(var i = 0; i < arr.length; i++) {
        if(arr > 85) {
           return A
        } else if (arr > 70 && arr < 85) {
           return B
        } else if (arr > 55 && arr < 70) {
           return C
        } else if (arr <= 55 && arr !== null) {
           return D
        } else if( arr === null) {
           return Empty
        }
    }
}

module.exports = convertScore